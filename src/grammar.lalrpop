use crate::ast::{Term};

grammar;

identifier = r"[[:alpha:]]";

pub Term: Term = {
  identifier => Term::Var(String::from(<>)),
  //(λx.M)
  "λ" <var: identifier> "." <body: Term> => Term::Abs(String::from(var), Box::new(body)),
}

